{
  "name": "tuev-card",
  "version": "0.1.0-b42",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build-bundle.mjs",
    "check": "node --check tuev-card.js && find src -name '*.js' -print0 | xargs -0 -n1 node --check"
  }
}
